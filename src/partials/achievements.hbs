{{#if resume.awards.length}}
<div class="container publications-container">
    {{> title value="Achievements and Certifications"}}

    {{#each resume.awards}}
    <section class="item">
        {{> section-header name=this.title}}

        {{#if publisher}}
        <h5 class="awarder">{{awarder}}</h5>
        {{/if}}

        {{#if summary}}
        <p class="summary">{{summary}}</p>
        {{/if}}

    </section>
    {{/each}}
</div>
{{/if}}