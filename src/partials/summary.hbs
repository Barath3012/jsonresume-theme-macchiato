{{#resume.basics}}
    {{#if summary}}
        <div class="container summary-container">
            {{> title value="About Me"}}
            <p class="summary">
                {{summary}}
            </p>
        </div>
    {{/if}}
{{/resume.basics}}